<footer class="bg-white shadow-sm dark:bg-gray-900 w-full">
    <div class="w-full max-w-screen-xl mx-auto p-4">
        <!-- Main footer content -->
        <div class="grid grid-cols-1 md:grid-cols-3 gap-8 py-8">
            <!-- Identity & Rights -->
            <div class="text-center md:text-left">
                <h3 class="text-lg font-semibold text-gray-900 dark:text-white mb-3">Estimatick</h3>
                <p class="text-sm text-gray-600 dark:text-gray-400 mb-2">{{ 'footer.slogan' | trans }}</p>
                <p class="text-xs text-gray-500 dark:text-gray-400">{{ 'footer.copyright' | trans }}</p>
            </div>

            <!-- Quick Navigation -->
            <div class="text-center">
                <h4 class="text-sm font-semibold text-gray-900 dark:text-white mb-3 uppercase tracking-wide">{{ 'footer.navigation.title' | trans }}</h4>
                <ul class="space-y-2 text-sm">
                    <li><a href="{{ path('app_home') }}" class="text-gray-600 dark:text-gray-400 hover:text-gray-900 dark:hover:text-white hover:underline">{{ 'footer.navigation.home' | trans }}</a></li>
                    <li><a href="#" class="text-gray-600 dark:text-gray-400 hover:text-gray-900 dark:hover:text-white hover:underline">{{ 'footer.navigation.features' | trans }}</a></li>
                    <li><a href="#" class="text-gray-600 dark:text-gray-400 hover:text-gray-900 dark:hover:text-white hover:underline">{{ 'footer.navigation.documentation' | trans }}</a></li>
                </ul>
            </div>

            <!-- Contact & Support -->
            <div class="text-center md:text-right">
                <h4 class="text-sm font-semibold text-gray-900 dark:text-white mb-3 uppercase tracking-wide">{{ 'footer.contact.title' | trans }}</h4>
                <div class="space-y-2 text-sm">
                    <p><a href="#" class="text-gray-600 dark:text-gray-400 hover:text-gray-900 dark:hover:text-white hover:underline">{{ 'footer.contact.form' | trans }}</a></p>
                    <p><a href="mailto:support@estimatick.com" class="text-gray-600 dark:text-gray-400 hover:text-gray-900 dark:hover:text-white hover:underline">support@estimatick.com</a></p>
                </div>

                <!-- Language Selector (Optional) -->
                <div class="mt-4">
                    <select class="text-xs bg-transparent border border-gray-300 dark:border-gray-600 rounded px-2 py-1 text-gray-600 dark:text-gray-400">
                        <option value="fr">🇫🇷 Français</option>
                        <option value="en">🇬🇧 English</option>
                    </select>
                </div>
            </div>
        </div>

        <!-- Separator -->
        <hr class="my-6 border-gray-200 sm:mx-auto dark:border-gray-700"/>

        <!-- Bottom section -->
        <div class="flex flex-col sm:flex-row sm:items-center sm:justify-between space-y-4 sm:space-y-0">
            <!-- Legal Links -->
            <ul class="flex flex-wrap items-center justify-center sm:justify-start text-sm font-medium text-gray-500 dark:text-gray-400">
                <li>
                    <a href="#" class="hover:underline me-4 md:me-6">{{ 'footer.links.legal_notice' | trans }}</a>
                </li>
                <li>
                    <a href="#" class="hover:underline me-4 md:me-6">{{ 'footer.links.privacy_policy' | trans }}</a>
                </li>
            </ul>

            <!-- Made with love badge -->
            <div class="flex items-center justify-center sm:justify-end text-xs text-gray-500 dark:text-gray-400">
                <span>{{ 'footer.made_with_love' | trans }}</span>
            </div>
        </div>
    </div>
</footer>
